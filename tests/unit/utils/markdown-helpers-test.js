import markdownHelpers from '../../../utils/markdown-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | markdown helpers');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = markdownHelpers();
  assert.ok(result);
});
