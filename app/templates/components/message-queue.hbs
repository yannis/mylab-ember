{{#if untimedMessages}}
  {{#each untimedMessages key='@guid' as |message|}}
    {{flash-message
      message=message
    }}
  {{/each}}
{{/if}}

{{#if currentMessage}}
  {{flash-message
    message=currentMessage
  }}
{{/if}}
